pages:
  stage: deploy
  image: node:latest
  cache:
    paths:
      - node_modules/
  before_script:
    - npm install -g docsify-cli
  script:
    - docsify serve docs
    - mkdir -p public
    - cp -r docs/* public/
  artifacts:
    paths:
      - public
  only:
    - main